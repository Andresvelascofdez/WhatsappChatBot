{
  "version": 2,
  "installCommand": "pnpm install --frozen-lockfile=false",
  "builds": [
    {
      "src": "api/chatbot.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/api/chatbot.js" }
  ]
}
